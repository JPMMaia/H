module Offset_pointer;

function take(external_pointer: *mutable Int32) -> ()
{
}

export function pointers(external_pointer: *mutable Int32) -> (result: *mutable Int32)
{
    mutable p0 = offset_pointer(external_pointer, 2i64);
    *p0 = 0;

    take(offset_pointer(external_pointer, 4i64));

    return offset_pointer(external_pointer, 6i64);
}
