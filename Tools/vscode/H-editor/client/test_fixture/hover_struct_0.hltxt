module hover_struct_0;

// Represents complex numbers. Uses 32-bit floats.
struct Complex
{
    // The real part.
    real: Float32 = 0.0f32;

    // The imaginary part.
    imaginary: Float32 = 0.0f32;
}

function run(input: My_struct) -> (result: My_struct)
{
    var instance: My_struct = {};
    return instance;
}
